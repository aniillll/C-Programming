#include<stdio.h>
#include<string.h>
#define SIZE 100
 main()
 {
 char a[SIZE];
 int x,y;
 printf("enter x and y values");
 scanf("%d %d",&x,&y);
 
 sprintf(a,"Anýl ilk sýnavdan %d ikinci sýnavdan %d aldý",x,y);
 printf("%s",a);
 	
 // SPRÝNTF bir dizinin içine bir metin atma iþlemidir.X VE Y deðiþkenlerini kullanarak.
 // printf le diziyi bastýrdýðýmýzda artýk içinde yeni dizi olmuþ olacak.	
 	
 	
 	
 	
 	
 	
 	
 	
 }
