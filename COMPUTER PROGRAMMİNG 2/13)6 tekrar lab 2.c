#include<stdio.h>
#include<stdlib.h>
#include<time.h>
 /*int largest(int *aptr,int size);
 main()
 {
 	srand(time(NULL));
 	int a[10],i,result;
 	for(i=0;i<10;i++)
 	{
 	a[i]=rand()%10;
 	printf("a[%d]=%d,",i,a[i]);	
	 }
 	
 	
 	result=largest(a,10);
	printf("\nlargest number is %d \n",result);
 	
 }
 int largest(int *aptr,int size)
 {
 int max;
   max=*(aptr);
 int i;	
 	for(i=1;i<size;i++)
 	{
 		if(*(aptr+i)>max)
 		{
 			
 		max=*(aptr+i);	
		 }
	 }
 	
 	return max;
 }
 */
 void reverse (int *aptr);
 main()
 {
 int a[10]={,3,5,8,7,6,78,45,57,90,100},i;
 for(i=0;i<10;i++)
 printf("a[%d]=%d,",i,a[i]);
 	
 reverse (a);	
 	
 }
 void reverse (int *aptr)
 {
 	int i,j,temp,hold;
 	
 	for(i=0;i<5;i++)
 	{
 		hold=(aptr+j)
 		for(j=10;j>5;j--)
 		{
 		  	
 			
 			
		 }
 	 	
 		
 		
	 }
 	
 	
 }
